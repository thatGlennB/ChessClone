export interface NewUser {
    Username:string;
    // 8 characters, one capital letter, one number
    Password:string;
    Email:string;
    Theme:number;
    Premium:boolean;
    Notify:boolean;
    SkillLevel:number;
 }
